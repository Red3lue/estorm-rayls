{
  "name": "contracts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "test:verbose": "forge test -vvv",
    "fmt": "forge fmt",
    "fmt:check": "forge fmt --check",
    "deploy:private": "forge script script/Deploy.s.sol --rpc-url $PRIVACY_NODE_RPC_URL --broadcast --legacy",
    "deploy:public": "forge script script/DeployPublic.s.sol --rpc-url $PUBLIC_CHAIN_RPC_URL --broadcast --legacy",
    "balance": "forge script script/CheckBalance.s.sol --rpc-url $PRIVACY_NODE_RPC_URL"
  }
}
